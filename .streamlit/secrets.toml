# Security credentials for the app login
[passwords]
# The app expects the password "AIAF_2026!" for the username "AIAF_Ops"
# You can change these here if you update the check_password() function in app.py
user = "AIAF_Ops"
password = "AIAF_2026!"

# API Key for Groq (AI Analysis)
# Replace "gsk_..." with your actual Groq API key
GROQ_API_KEY = "...."
